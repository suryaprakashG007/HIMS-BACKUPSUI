# apiVersion: v1
# kind: Service
# metadata:
#   name: crater-nginx
# spec:
#   type: ClusterIP
#   selector:
#     app: crater-nginx
#   ports:
#     - name: http         
#       port: 80
#       targetPort: 80
#     - name: https        
#       port: 443
#       targetPort: 443

apiVersion: v1
kind: Service
metadata:
  name: proxy   # <-- this must match your LoadBalancer service
spec:
  type: LoadBalancer
  selector:
    app: crater-nginx  # must match crater-nginx pods
  ports:
    - name: http         
      port: 80
      targetPort: 80
    - name: https        
      port: 443
      targetPort: 443

# apiVersion: v1
# kind: Service
# metadata:
#   name: crater-nginx
# spec:
#   type: NodePort
#   selector:
#     app: crater-nginx
#   ports:
#     - name: http
#       port: 80
#       targetPort: 80
#       nodePort: 30444  # Must be between 30000–32767
