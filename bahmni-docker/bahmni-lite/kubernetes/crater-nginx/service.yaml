apiVersion: v1
kind: Service
metadata:
  name: crater-nginx
spec:
  selector:
    app: crater-nginx
  ports:
    - protocol: TCP
      port: 444              # External port for ELB
      targetPort: 80         # Nginx usually listens on 80
  type: LoadBalancer         # 👈 This is what creates the ELB


# apiVersion: v1
# kind: Service
# metadata:
#   name: crater-nginx
# spec:
#   type: NodePort
#   selector:
#     app: crater-nginx
#   ports:
#     - name: http
#       port: 80
#       targetPort: 80
#       nodePort: 30444  # Must be between 30000–32767
