apiVersion: v1
kind: Service
metadata:
  name: crater-atomfeed-db-mysql
  labels:
    app: crater-atomfeed-db-mysql
spec:
  selector:
    app: crater-atomfeed-db-mysql
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP
