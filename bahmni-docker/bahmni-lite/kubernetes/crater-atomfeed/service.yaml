apiVersion: v1
kind: Service
metadata:
  name: crater-atomfeed
  labels:
    app: crater-atomfeed
spec:
  type: LoadBalancer
  selector:
    app: crater-atomfeed
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
